% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare_query.R
\name{prepare_query}
\alias{prepare_query}
\alias{print.trafa_query}
\title{Prepare a data query with progressive validation}
\usage{
prepare_query(
  product,
  measure,
  ...,
  lang = NULL,
  validate = TRUE,
  verbose = FALSE
)

\method{print}{trafa_query}(x, ...)
}
\arguments{
\item{product}{Character: product code (e.g. \code{"t10011"}).}

\item{measure}{Character: measure name (e.g. \code{"itrfslut"}).}

\item{...}{Ignored.}

\item{lang}{Language code: \code{"SV"} or \code{"EN"}.}

\item{validate}{Logical. If \code{TRUE} (default), validates dimension
compatibility against the API structure.}

\item{verbose}{Print request details.}

\item{x}{A \verb{<trafa_query>} object.}
}
\value{
A \verb{<trafa_query>} object. Pass to \code{\link[=get_data]{get_data()}} via \code{query}.
}
\description{
Fetches the product structure, validates that the requested measure and
dimensions are compatible, and returns a query object that can be passed
to \code{\link[=get_data]{get_data()}}.
}
\details{
The Trafa API supports progressive structure discovery: adding a measure
to the structure query reveals which dimensions are valid for that measure
(via the \code{option} field). This function leverages that to warn about
invalid dimension combinations before data is requested.
}
\examples{
\donttest{
if (trafa_available()) {
  q <- prepare_query("t10011", "itrfslut", ar = "2024")
  q

  get_data(query = q)
}}
}
