% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/measures.R
\name{get_measures}
\alias{get_measures}
\title{Get measures (KPIs) for a product}
\usage{
get_measures(
  product,
  lang = NULL,
  cache = FALSE,
  cache_location = trafa_cache_dir,
  verbose = FALSE
)
}
\arguments{
\item{product}{Character: product code (e.g. \code{"t10011"}).}

\item{lang}{Language code: \code{"SV"} or \code{"EN"}.}

\item{cache}{Logical, cache results locally.}

\item{cache_location}{Cache directory. Defaults to \code{\link[=trafa_cache_dir]{trafa_cache_dir()}}.}

\item{verbose}{Print request details.}
}
\value{
A tibble with columns: \code{product}, \code{name}, \code{label}, \code{description},
\code{id}, \code{unique_id}, \code{option}, \code{active_from}, \code{data_type}, \code{parent_name}.
}
\description{
Retrieves the available measures for a Trafa product. Each measure
represents a specific statistic (KPI) that can be queried with
\code{\link[=get_data]{get_data()}}. A product typically has several measures — for example,
"Bussar" (t10011) has measures for vehicles in traffic, deregistered,
newly registered, etc.
}
\examples{
\donttest{
if (trafa_available()) {
  get_measures("t10011") |> measure_describe()
}}
}
