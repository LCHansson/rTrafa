% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/products.R
\name{product_has_data}
\alias{product_has_data}
\title{Check if a product is a data-bearing leaf or an empty container}
\usage{
product_has_data(
  product,
  lang = NULL,
  cache = FALSE,
  cache_location = trafa_cache_dir,
  verbose = FALSE
)
}
\arguments{
\item{product}{Character: product code.}

\item{lang, cache, cache_location, verbose}{Standard rTrafa args.}
}
\value{
Logical: \code{TRUE} if the product's data endpoint returns rows,
\code{FALSE} if it appears to be an empty container.
}
\description{
The Trafa API does not model parent-child relationships between products
explicitly. However, some products (e.g. "Fordon på väg", t10010) have
dimensions and measures in their structure but return no data rows — they
act as organizational containers. This function checks whether a product
has actual data by inspecting its structure for dimension/measure items
whose \code{parent_name} matches the product code, and then verifying whether
the data endpoint returns rows.
}
\details{
\strong{Note:} when a product is a container, the related "sub-products"
cannot be discovered programmatically via the API. Use
\code{\link[=product_search]{product_search()}} on the product catalogue to find products with
similar names (e.g. \code{product_search(get_products(), "fordon")}).
}
\examples{
\donttest{
if (trafa_available()) {
  product_has_data("t10011")   # TRUE — Bussar has data
  product_has_data("t10010")   # FALSE — container, no data rows
}}
}
