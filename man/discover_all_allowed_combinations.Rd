% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discover_combinations.R
\name{discover_all_allowed_combinations}
\alias{discover_all_allowed_combinations}
\title{Discover allowed combinations for every Trafa product}
\usage{
discover_all_allowed_combinations(
  cache = TRUE,
  cache_location = NULL,
  verbose = FALSE
)
}
\arguments{
\item{cache}{logical(1). Passed to \code{\link[=discover_allowed_combinations]{discover_allowed_combinations()}}.
Default \code{TRUE}.}

\item{cache_location}{nordstatExtras handle passed through.}

\item{verbose}{logical(1). Print per-product progress.}
}
\value{
Combined tibble with the same columns as
\code{\link[=discover_allowed_combinations]{discover_allowed_combinations()}}.
}
\description{
Loops \code{\link[=get_products]{get_products()}} and calls \code{\link[=discover_allowed_combinations]{discover_allowed_combinations()}}
for each. Per-product failures are caught and surfaced via
\code{\link[=warning]{warning()}} so the batch run completes even when individual products
fail.
}
\details{
First-pass runtime: ~10-20 minutes for ~57 products (depending on
product complexity and Trafa API latency). Subsequent passes with
the same \code{cache_location} are near-instant — each \code{get_dimensions()}
call is cached individually.
}
\examples{
\dontrun{
handle <- nordstatExtras::nxt_open("su_cache.sqlite")
combos <- discover_all_allowed_combinations(cache_location = handle,
                                            verbose = TRUE)
nordstatExtras::nxt_close(handle)
}

}
