% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dimensions.R
\name{get_dimensions}
\alias{get_dimensions}
\title{Get dimensions (filter variables) for a product}
\usage{
get_dimensions(
  product,
  measure = NULL,
  only_valid = TRUE,
  lang = NULL,
  cache = FALSE,
  cache_location = trafa_cache_dir,
  verbose = FALSE
)
}
\arguments{
\item{product}{Character: product code (e.g. \code{"t10011"}).}

\item{measure}{Character: optional measure name. When provided, only
dimensions valid for that measure are returned (unless
\code{only_valid = FALSE}).}

\item{only_valid}{Logical. When \code{measure} is provided and
\code{only_valid = TRUE} (default), dimensions with \code{option = FALSE} are
excluded. Set to \code{FALSE} to see all dimensions with their validity
status.}

\item{lang}{Language code: \code{"SV"} or \code{"EN"}.}

\item{cache}{Logical, cache results locally.}

\item{cache_location}{Cache directory. Defaults to \code{\link[=trafa_cache_dir]{trafa_cache_dir()}}.}

\item{verbose}{Print request details.}
}
\value{
A tibble with columns: \code{product}, \code{name}, \code{label}, \code{data_type},
\code{option}, \code{description}, \code{hierarchy}, \code{n_values}, \code{values}.

The \code{values} column contains nested tibbles with columns \code{name},
\code{label}, and \code{type} (\code{"value"} for regular dimension values,
\code{"filter"} for API filter shortcuts like \code{"senaste"} / latest).
}
\description{
Retrieves the available dimensions for a Trafa product. Dimensions are
the categorical variables you can filter on when fetching data with
\code{\link[=get_data]{get_data()}} — for example year, fuel type, or owner category.
}
\details{
Dimensions that belong to a hierarchy (e.g. "Ägarkategori" under the
"Ägare" hierarchy) are included with their hierarchy noted in the
\code{hierarchy} column. Hierarchies themselves are not queryable — only
their child dimensions are.

When \code{measure} is provided, the API validates which dimensions are
compatible with that measure. Invalid dimensions are excluded by default
(controlled by \code{only_valid}).
}
\examples{
\donttest{
if (trafa_available()) {
  # All dimensions
  get_dimensions("t10011") |> dimension_describe()

  # Validated against a specific measure
  get_dimensions("t10011", measure = "itrfslut")

  # Inspect values for a dimension
  get_dimensions("t10011") |> dimension_values("drivm")
}}
}
