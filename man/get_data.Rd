% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\name{get_data}
\alias{get_data}
\title{Fetch data from the Trafa API}
\usage{
get_data(
  product,
  measure,
  ...,
  query = NULL,
  lang = NULL,
  simplify = TRUE,
  cache = FALSE,
  cache_location = NULL,
  verbose = FALSE
)
}
\arguments{
\item{product}{Character: product code (e.g. \code{"t10011"}).
Ignored when \code{query} is provided.}

\item{measure}{Character: measure name (e.g. \code{"itrfslut"}).
Ignored when \code{query} is provided.}

\item{...}{Dimension filters as named arguments. Each name is a dimension
name, each value is a character vector of filter values.
Unspecified dimensions return all values.
Ignored when \code{query} is provided.}

\item{query}{A \verb{<trafa_query>} object from \code{\link[=prepare_query]{prepare_query()}}. When provided,
\code{product}, \code{measure}, and \code{...} are taken from the query object.}

\item{lang}{Language code: \code{"SV"} or \code{"EN"}.}

\item{simplify}{Add human-readable label columns alongside codes.}

\item{cache}{Logical. If \code{TRUE} and \code{cache_location} points at a SQLite
file (or an \code{nxt_handle} from nordstatExtras), the data is cached at
cell granularity in that database. Supports concurrent multi-process
read/write and cross-query cell reuse. Requires \code{nordstatExtras}.}

\item{cache_location}{Either a path to a \code{.sqlite} file or an \code{nxt_handle}
returned by \code{nordstatExtras::nxt_open()}. Ignored unless \code{cache = TRUE}.}

\item{verbose}{Print request details.}
}
\value{
A tibble of data. Dimension columns use the dimension name;
when \code{simplify = TRUE}, additional \verb{\{name\}_label} columns are added.
Measure values are in a column named after the measure.
}
\description{
The core function for downloading statistical data. Dimension filters
are passed as named arguments via \code{...}, or via a prepared query object
from \code{\link[=prepare_query]{prepare_query()}}.
}
\examples{
\donttest{
if (trafa_available()) {
  # Direct fetch
  get_data("t10011", "itrfslut", ar = "2024")

  # With filters
  get_data("t10011", "itrfslut",
    ar = c("2023", "2024"),
    drivm = c("102", "103"))

  # From a prepared query
  q <- prepare_query("t10011", "itrfslut", ar = "2024")
  get_data(query = q)
}}
}
