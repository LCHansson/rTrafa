% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discover_combinations.R
\name{discover_allowed_combinations}
\alias{discover_allowed_combinations}
\title{Discover allowed variable combinations via the Trafa API}
\usage{
discover_allowed_combinations(
  product,
  measure = NULL,
  cache = TRUE,
  cache_location = trafa_cache_dir,
  verbose = FALSE
)
}
\arguments{
\item{product}{character(1). Trafa product code (e.g. "t10011").}

\item{measure}{character vector. One or more measure codes. When
\code{NULL} (default) or longer than one, the function iterates over
each measure and concatenates the results.}

\item{cache}{logical(1). Cache \code{get_dimensions()} results via
nordstatExtras. Default \code{TRUE} — discovery makes many API calls per
product so caching is essential for reasonable runtime on re-runs.}

\item{cache_location}{nordstatExtras handle (or a \code{trafa_cache_dir})
passed to \code{\link[=get_dimensions]{get_dimensions()}}.}

\item{verbose}{logical(1). Print per-query diagnostics: product,
measure, current dim-set, and addable dimensions found.}
}
\value{
A tibble with columns:
\itemize{
\item \code{product}: character. Same as input.
\item \code{measure}: character. Measure code.
\item \code{dimensions}: list-col of character vectors. Each entry is a valid
dimension-set (always includes \code{"ar"}) for the given
\verb{(product, measure)}.
}

One row per \verb{(product, measure, dim-set)} combination.
}
\description{
Enumerates all \verb{(measure, dim-set)}-combinations that the Trafa API
will accept for a given product, by recursively probing the structure
endpoint via \code{\link[=get_dimensions]{get_dimensions()}} with a growing measure-and-dimension
vector. The stopping condition is when the API returns no new
addable dimensions beyond the implicit \code{"ar"} (year).
}
\details{
This replaces the earlier HTML-scraping approach (which read Trafa's
documentation pages directly) with a proper API-driven enumeration.
Same enumeration semantics, but using the documented \verb{/api/structure}
endpoint instead of brittle CMS markup.
}
\examples{
\dontrun{
discover_allowed_combinations("t10011", "itrfslut")
discover_allowed_combinations("t10011")  # all measures
}

}
\seealso{
\code{\link[=discover_all_allowed_combinations]{discover_all_allowed_combinations()}} for batch discovery
across all products.
}
